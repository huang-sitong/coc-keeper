[project]
name = "coc-keeper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "huang-sitong", email = "huang-sitong@outlook.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
coc-keeper = "coc_keeper:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
